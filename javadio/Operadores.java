package javadio;
public class Operadores {
    //'++' e '--' sufixo utilizado para adicionar 1 em uma variável integer.Se usada antes da variável (ex: ++numero), adiciona o valor antes do print da variável. Se usada após (ex: numero++), adiciona o valor após o print da variável. Ou seja, o valor novo só será impresso no próximo 'print'.
    // &&(and) e ||(or) condicionais.
    //    
}
